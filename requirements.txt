# ============================================================
# DriverAid-AI  -  Streamlit Community Cloud requirements
# ------------------------------------------------------------
# NOTE on OpenCV: MediaPipe hard-depends on opencv-contrib-python (GUI build),
# which needs system libs (libGL/libgthread) that aren't on Streamlit Cloud.
# We do NOT use packages.txt (the apt base image is unstable). Instead, the
# headless guard at the TOP of app.py swaps cv2 to opencv-python-headless at
# startup. Do not pin an opencv line here - let MediaPipe resolve it, the
# guard fixes it at runtime.
# ============================================================

# --- Core app + webcam transport ---
streamlit==1.39.0
streamlit-webrtc==0.47.0

# --- Computer vision (pulls opencv-contrib-python automatically) ---
mediapipe==0.10.14

# --- Deep learning (CPU build: smaller + fits Community Cloud) ---
# tf.keras ships INSIDE tensorflow. Do NOT also install standalone `keras`,
# it pulls Keras 3 and silently changes the .keras save/load format.
tensorflow-cpu==2.16.1

# --- Numerics / utils (pinned for TF 2.16 + MediaPipe 0.10 compatibility) ---
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
Pillow==10.3.0
