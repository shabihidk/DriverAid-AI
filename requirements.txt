# Force Headless OpenCV first
opencv-python-headless==4.9.0.80

# WebRTC for the webcam
streamlit-webrtc==0.47.0

# Core Data/ML
streamlit
numpy
pandas
scikit-learn
Pillow

# Deep Learning (Keep these exact versions if possible, or omit versions if they clash)
tensorflow
keras
mediapipe